# HAL Engine on npm — tasks

Ordered by dependency. `[P]` marks a task with no unmet dependency inside its
own phase, so it can run alongside its siblings. Ids are assigned on creation
and never reused, so a task added later sits where the dependencies put it
rather than at the end.

Two hard sequencing facts govern everything below. `npm publish` cannot be
undone, so T031 runs last. And the repository-visibility ADR (T006) decides
whether the publish carries `--provenance`, so it is started first rather than
left until the workflow needs it.

## Phase 1 — The package

The module format and the manifest. Nothing downstream can be written until
`dist/` is the shape the release will carry.

- [x] T001 Convert to ESM-only. Add `"type": "module"` to `package.json`; resolve the five `require()` calls in `src/providers/providerFactory.ts` while keeping per-arm lazy loading; make `jest.config.js` an ESM config and run the suite under `--experimental-vm-modules`; give the `dev` and `test` scripts an ESM-compatible loader; update the add-a-provider recipe's steps 2 and 3 in `specs/hal-engine-providers/spec.md`, whose relative imports carry no `.js`. Migration guide in the PR body — AGENTS.md § Breaking Changes.
- [x] T002 Make the optional peers optional. Move `SchemaType` into the `import type` block in `src/providers/vertex/vertexProvider.ts` and lazy-load `VertexAI` inside `createVertexProvider`, mirroring `src/providers/bedrock/bedrockProvider.ts`. Add a `requireOptionalPeer` helper in `src/shared/` that both providers route through, so an absent peer produces one `AIError`-shaped message naming the package and its install command. No subpath exports. Depends on T001.
- [x] T003 [P] Forward `MockConfig` through the factory. `src/providers/providerFactory.ts:34` passes `config` like the other four arms. Add `src/providers/providerFactory.test.ts` — the factory has no test file today, which is how a one-word omission reached a release-candidate backlog. Leave `createMockProvider`'s parameter optional.
- [x] T004 Bring `package.json` to the publishing convention. Rename to `@re-cinq/hal-engine`, bump to `0.2.0`, add `license`/`repository`/`exports`/`publishConfig`/`engines`, keep `main`/`types`/`files`/`prepare`, add a root `LICENSE` (Apache-2.0), regenerate `package-lock.json`. `bugs` arrives with T018. Depends on T001.
- [x] T005 [P] Reconcile the context files. `.specify/spec.md:276` (CommonJS → ESM through the `exports` map), `:278` (`hal-engine` → `@re-cinq/hal-engine`), `:9` (drop the version — `package.json` is where one lives); `AGENTS.md:320` (ISC → Apache-2.0, and resolve the per-file header clause); name `.specify/spec.md` in § Documentation Requirements. Both files are ingested by context tooling and served as authority, so stale here means the pipeline building the next change disagrees with the manifest. Depends on T004.

## Phase 2 — The release pipeline

CI gates first, then the workflow that uses them. T006 gates the release
workflow, so it should be opened at the start of this phase, not at its end.

- [x] T006 **Blocker.** Record the repository-visibility decision in `adrs/`: publishing with `--provenance` requires a publicly resolvable source repository, so the release workflow either carries the flag or it does not. State the rule generally enough that a later package reads it rather than re-deriving it, and frame the trade honestly — the built package is public either way, so visibility decides the history, the tests, the examples and the issue record, and exposure covers every commit rather than the tip. T011 cannot be finished until this lands.
- [x] T007 [P] Commit a coverage floor. Add `collectCoverageFrom` and `coverageThreshold` to `jest.config.js`, a `test:coverage` script, and a call site in `.github/workflows/ci.yml` (`coverage/` is already gitignored). Set the floor exactly at the measured baseline — 67.96% statements, 57.54% branches, 68% functions, 68.34% lines at `3186f07` — and treat it as a ratchet.
- [x] T008 [P] Audit the build chain from a job that fails. Add `scripts/check-audit.mjs` (full installed tree, no `--omit=dev`, fails at `high` and above, reads committed acceptances carrying advisory/package/reason/ISO expiry/acknowledged-by, an expired entry fails too) and `scripts/check-action-pins.sh` (every `uses:` is a 40-hex SHA — the three workflows present today are not). Add a `check:build-chain` script and a CI job. Keep `supertest` — `src/transport/routes/chats.test.ts` imports it, so an earlier zero-occurrence measurement no longer holds. Baseline before the work: 18 advisories, 1 critical / 6 high; `npm audit fix` takes it to 3, none at high or above, lockfile only.
- [x] T009 Smoke-test the packed tarball. Commit a `smoke/` consumer fixture (own `package.json` with `private: true` and `"type": "module"`, an ESM entry point, a TypeScript file importing the types, its own `tsconfig.json`); add `scripts/smoke.sh` that packs, installs into a temp dir **outside** the repository tree, and runs it with no optional peer installed; add a CI job; exclude `smoke/` from `tsconfig.json` and `tsconfig.build.json` the way `example` already is (the jest roots never reach it). Commit the fixture rather than generating it in YAML, so it runs on a laptop too. Depends on T002, T003, T004.
- [x] T010 [P] Settle the release-notes convention. Add `CHANGELOG.md` (Keep a Changelog 1.1.0, `## [Unreleased]` first) with entries for the ESM conversion and the scope rename written for a consumer, not a committer. Replace `AGENTS.md:330` with the real rule. Add a checklist box to `.github/PULL_REQUEST_TEMPLATE.md` and a CI step that fails when a diff touching `src/` leaves `CHANGELOG.md` untouched, with a label as the escape hatch.
- [ ] T011 Publish from a `v*` tag. Add `.github/workflows/publish.yml` (Trusted Publishing over OIDC, no `NPM_TOKEN`, `--access public`, `--provenance` per T006's answer) and `scripts/check-version.sh` (tag vs. committed version, a script so it runs before a tag is pushed). Call the T008 and T009 checks before `npm publish`, and add a step that fails when the pack list contains any `*.test.*` entry. Add § Releasing to `AGENTS.md`: bump in the PR, merge, tag `vX.Y.Z` on `main`, push. `npm ci --ignore-scripts` throughout, because `prepare` runs on both `ci` and `publish`. Depends on T009, T010, T006.

- [ ] T032 [P] Alarm on a commit that reaches `main` outside a pull request. Add `.github/workflows/guard-main-pushes.yml` — `on: push: branches: [main]`, least privilege (`contents: read`, `issues: write`, `pull-requests: read`), and a job that resolves each pushed commit to its associated pull requests and opens one issue per commit that has none, naming the sha, the author and the committer. Branch protection is the lock and this is the detector behind it: `main` requires a PR, blocks force-pushes and deletion, and enforces all of it for admins, but a protection rule that is relaxed or bypassed leaves no alarm of its own. It matters in this repository specifically because T011 authenticates to the registry over OIDC with nothing stored to revoke, so push access to `main` is transitively the right to publish under the scope. Must land before T031; depends on nothing.

## Phase 3 — Correctness the first release must not ship

Independent of phase 2 and runnable alongside it. All of it must land before
T031, because none of it can be fixed in a published version.

- [ ] T012 Deny unauthenticated requests to the demo chat routes. `src/transport/routes/chats.ts:33` — the pass-through fallback becomes a `401` middleware, across all three routes. Signatures unchanged. Correct `specs/hal-engine-chat-routes/spec.md` § When the guard does nothing, which currently states the old default as behaviour. Add a comment to `example/server.ts` noting that its chat route now answers 401.
- [ ] T013 Enforce ownership. Export `AuthenticatedRequest` from `src/types/auth.ts`, re-export from `src/types/index.ts` and `src/index.ts`, restate `HttpAuthMiddleware` in its terms. Replace the four inline casts in `chats.ts` and drop the `?? 'anonymous'` fallback at `:35`. Add a `requireUser` guard answering `401` when `req.user` is absent or its `id` is `undefined`, `null` or `''` — the empty string named explicitly, because `id` is `string | number` and `0` is a legal id. Depends on T012.
- [ ] T014 [P] Say what the routes are. A file-head comment in `src/transport/routes/chats.ts` naming the process-local `Map`, stating `_sessionStore` is unused by design, and stating that a REST-created chat id is not a WebSocket session id. Export and parameter both stay. One line per comment — `re-lint/max-comment-lines`.
- [ ] T015 [P] Call `onConnect`. Add it to the `deps` object at `src/transport/createServer.ts:50` (its sibling is forwarded on the adjacent line) and to `ConnectionHandlerDeps`; invoke it after the `connected` frame through a deferred continuation, never inline in the `connection` listener — a synchronous throw there corrupts an already-upgraded socket. Widen both hooks to `void | Promise<void>`, route both through one `runHook` helper that logs `{sessionId, error}` and swallows, and fix the four documents that describe the hook, including the example that reads a session field that does not exist.
- [ ] T016 [P] Emit structured log lines. `emit` in `src/shared/logger.ts` writes one JSON object per line — `severity`, `message`, ISO `timestamp`, `category`, `data` — with `ERROR` to `console.error` and the rest to `console.log`. Add `src/shared/logger.test.ts` asserting the format contract, and `docs/logging.md` covering the key set, the level mapping, the stream split and the person-identifying fields. No call site changes: all 28 `log.*` calls keep their level, category, message and data.
- [ ] T017 **Decision, then change.** `src/transport/ws/validation.ts:31` accepts `send_message` and normalises it away, so the exported union can never contain it; `specs/hal-engine-websocket-protocol/spec.md:98,109` documents the alias as supported. An earlier decision said remove it from the code; the spec restructure resolved it the other way. Decide once, then make code, exported types and spec agree. A deprecation window is required if any client is sending the alias.
- [ ] T018 [P] Give a reporter somewhere to go. Add `SECURITY.md` (intake channel, acknowledgement target, supported versions written for a pre-1.0 package with no maintenance branch, coordinated disclosure, and an in-scope list naming at minimum `src/transport/`, `src/orchestration/tools/` and `src/providers/`); a `## Security` section in `README.md` repeating the address verbatim; a `bugs` field in `package.json`; a security contact link in `.github/ISSUE_TEMPLATE/config.yml`. The address must live somewhere that works even if the repository stays private.

## Phase 4 — What the published docs say

T019 through T022 are the package's npm front page and gate T031. The rest can
land after the first publish.

- [ ] T019 Make the front page and the guide match the code. Rewrite the `README.md` quick start and the `docs/getting-started.md` minimal setup against `example/server.ts` line for line. Correct `PromptBuilderConfig` at `docs/getting-started.md:82,114,115` — `guidelines: [...]` → `responseGuidelines` as one string, `context:` → `domainContext:`, `customInstructions:` → `toolPreamble:`; `specs/hal-engine-architecture/spec.md` is already right. Add `tsconfig.example.json`, a `typecheck:example` script and a CI call site, so `example/` stops being the one correct file nothing checks.
- [ ] T020 [P] Score the provider table per method. Replace the single status word in `README.md` § Providers with a `sendMessage` / `generateStructured` pair, each cell reading `Implemented` or the literal thrown string. Fix the same single-axis claim at `README.md:9` and in `.specify/spec.md`, and the contradiction between the Features list (Vertex "stub") and the table (Vertex `Full`). Qualify "switching providers is config-only": a consumer calling `generateStructured` cannot move from Vertex to Bedrock. Keep the matrix in `README.md` only — npm renders nothing else, and a second copy guarantees drift.
- [ ] T021 [P] State which install path is supported. An `## Install` section above the quick start: the registry specifier is the product. One paragraph on the git specifier stating four measured facts — it builds `dist` from a checkout via `prepare`, carries no provenance, holds whatever commit the lockfile pinned, and installs under the dependency's **key** rather than its `name`, so the existing `"hal-engine": "github:…"` entry keeps resolving silently after T004 renames the package. Name the unit and audience of the deprecation clause in `AGENTS.md`: registry versions, and consumers resolving one.
- [ ] T022 [P] Document the environment variables that are actually read. Delete the `AWS_REGION` and `GOOGLE_CLOUD_PROJECT` rows rather than annotating them — both are config fields, and a reader who sets the variable and passes a conflicting config value gets the config value silently. Keep the vendor credential rows, each gaining a sentence naming the construction site and stating this package reads none of them. Add one three-row table to `README.md` § Configuration — `CORS_ORIGIN` (`src/transport/createApp.ts:23`), `PORT` (`src/transport/createServer.ts:87`), `LOG_LEVEL` (`src/shared/logger.ts:12`) — each row naming the reading site, what overrides it and the default, with `CORS_ORIGIN` documented as one origin only.
- [ ] T023 [P] Move the documented Vertex example to an EU region. `specs/hal-engine-providers/spec.md:86,110` and the fixture at `src/providers/vertex/vertexProvider.test.ts:26`. Add one sentence on what `location` decides and that the engine does not validate it. Verify at edit time that each snippet's model id is served in the chosen region and record the check in the PR body.
- [ ] T024 [P] Check the documented model ids against what the vendors serve. Five distinct ids across `specs/hal-engine-providers/spec.md`, `docs/getting-started.md` and two test fixtures. `modelId` is a bare unvalidated string on both working provider configs, so a retired id reads to a consumer as their own credentials being wrong. Depends on T023.
- [ ] T025 [P] Mark the tool-evaluation how-to as a design note. `git mv docs/adding-tool-evaluation.md docs/spikes/` and prepend a status block naming the measurement date and the claims that do not hold — yarn against an npm repository, a `scripts/` layout that does not match, an undefined config file and types, a JWT field that does not exist, and a false gitignore claim covering a path that would write unredacted booking data. Name where the design is actually being built. Do not correct the body.
- [ ] T026 [P] Mark the two spikes. A status blockquote after the H1 and before the first `##` in `docs/spikes/spike-ai-response-validation.md` and `docs/spikes/spike-bedrock-integration.md`, stating what shipped, what is superseded and what is still open. Nothing below the block changes. They were moved during the spec restructure but never marked, and they still rank top for an observability question.
- [ ] T027 [P] Correct the §8.1 frame order. In `specs/hal-engine-websocket-protocol/spec.md`, the tool `entry_upsert` frame moves before the thinking-entry `entry_commit`, and a sentence states that commit frames are not emitted in index order — a thinking entry commits when the next text segment arrives, not when a tool entry appears. Align to the pinned frame-contract fixture rather than a fresh hand transcription; hand transcription is what produced the bug.
- [ ] T028 [P] Verify the architecture spec. Largely discharged by the spec restructure — `0399b9d`, `1f46c19`, `6a65a47` — so this is a confirmation pass, not a rewrite. Check § Entry Streaming Protocol names `appendEntry`/`appendDelta`/`commitEntry` from `src/orchestration/entryMutations.ts` with their real return types and in-place mutation, and that the fourth frame type is present. Record it as done if it is.
- [ ] T029 Generate the documented TypeScript blocks. Add `scripts/check-doc-blocks.mjs` with `docs:check` and `docs:fix` scripts and a CI call site. Every fenced `typescript` block in `README.md`, `docs/getting-started.md` and `specs/hal-engine-architecture/spec.md` carries a marker naming either an exported source declaration or a `#region` span in a checked file under `example/`. An unmarked block fails; an uncheckable block carries a reasoned opt-out. Depends on T019.
- [ ] T030 Extend the extractor to the remaining documents. Roughly 70 blocks across eight files. Two known defects fall out of it: a documented `ToolExecutor` signature missing the parameter carrying `userId`, `sessionId` and `authHeaders`, and a complete tool example importing from a path that has never resolved from where the example sits. Depends on T029.

- [ ] T033 [P] Say how an outside contributor works here. Add `CONTRIBUTING.md`: the pre-commit sequence, the commit format and its scope list, the five-layer rule and the fact that a shortcut fails lint rather than review, the spec-and-ADR convention including what the header table's `Status` actually tracks, and the changelog entry T010 turned into a gate. State that a pull request from a fork runs a reduced set of checks, because a fork receives no repository secrets and a read-only token — otherwise a contributor reads a skipped advisory check as something they broke. Link to `AGENTS.md` and `CLAUDE.md` rather than restating what either already governs: a second copy of a rule is a second thing to keep true. Depends on T010.

## Phase 5 — First publish

One task, and it is irreversible.

- [ ] T031 Bootstrap tokenless publishing. In order: create a short-lived granular token and store it as `NPM_TOKEN`; add a single-use `workflow_dispatch` workflow; run it once so `@re-cinq/hal-engine@0.2.0` exists; register the trusted publisher against `publish.yml`; revoke the token, delete the secret, delete the single-use workflow; tag `v0.2.1` and confirm it publishes with no secret in the repository. Run the token publish from CI, not a laptop, so the provenance attestation comes from the GitHub OIDC token. Depends on T011, T032, all of phase 3, and T019–T022.

## Not scoped here

- Repairing any downstream consumer that imports the bare specifier `hal-engine`. That is the consuming project's work; type-only and value imports break together, because a module specifier resolves at compile time either way.
- Carrying a session id on the WebSocket upgrade request, which is what would let a REST-created chat id reach a socket. T014 documents the gap instead.
